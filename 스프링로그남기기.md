## [실습] 스프링 서버 로그 남기기

1. https://start.spring.io/

2. 프로젝트 이름 : linux-demo
```
spring boot 3.5.9
Packaging : jar
Configuration : yaml
Java : 21
```
3. 
```
dependencies {
  implementation 'org.springframework.boot:spring-boot-starter-web'
  developmentOnly 'org.springframework.boot:spring-boot-devtools'
  testImplementation 'org.springframework.boot:spring-boot-starter-test'
  testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}
```

4. 
```sh
# -p옵션은 부모와 상관없이. 중복되도 문제없이 생성
mkdir -p ~/workspace

cp -r /mnt/d/makcoding/10Linux/linux-demo ~/workspace/

cd workspace

cd linux-demo

java -version

sudo apt list --installed | grep java

# 기존 빌드된 파일을 삭제하고 새롭게 JAR로 빌드
# 새로 빌드가 되면 사용자 권한 바뀜
./gradlew clean build


ls -al로 조회시 rw- -> rwx변경

cd ./build/libs

chmod 744 linux-demo-0.0.1-SNAPSHOT.jar

# 서버 빠져나올 때 : Ctrl + X

cd ~ # /home/kiwi

cd ./workspace/linux-demo/src/main/resources/static

nano index.html

# 실행시킨 서버 종료하기
Ctrl + C 누르기

# 표준출력과 표준에러 출력을 파일로 남기기
# java : 자바 실행
# -jar : JAR파일을 실행모드로 실행
# linux-demo-0.0.1-SNAPSHOT.jar: 실행할 Spring Boot서버의 jar파일명
# >> : 추가(>하나이면 덮어쓰기)
# app.log는 로그를 저장할 파일 명
# 기존에 app.log가 있으면 뒤에 붙여서 출력함
# 없으면 새로 생성함.
# 1: 표준출력(stdout) - 일반로그
# 2: 표준에러(stderr) - 에러로그
# 2>&1: 에러 출력(2)을 표준출력(1)이 가는 곳과 같은
# 곳으로 보내라는 의미임
# 표준출력이란 명령어의 실행결과를 출력할 곳으로 이동시켜주는 통로임.
# 즉 표준 출력에 기본값으로 설정되어 있는 출력할 곳은 터미널 화면인데 이것을 다른곳으로도 보낼 수 있음.
java -jar linux-demo-0.0.1-SNAPSHOT.jar >> app.log 2>&1
```

